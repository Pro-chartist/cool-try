# GitHub Configuration
GITHUB_TOKEN=your_github_personal_access_token_here
NEXT_PUBLIC_GITHUB_REPO=your-username/screener-cloud

# Note: NEXT_PUBLIC_ prefix makes the variable available to the browser
# GITHUB_TOKEN is kept private (server-side only)
